# ZCode workspace metadata
.zcode/

# Work logs (local only, never pushed)
worklog/

# Windows
Thumbs.db
Desktop.ini

# GMod compile artifacts
*.gma

# Art originals (large, local archive only — final PNGs under content/ are tracked)
*_original.png
originals/
setting_packs/*/artwork/

# Binary VTF/VMT materials (generated from PNGs in content/ — do not push to GitHub)
materials/**/*.vtf
materials/**/*.vmt
materials/**/*.png

# Sound binaries (large, local only)
gamemodes/fireteam/content/sound/**/*.wav
gamemodes/fireteam/content/sound/**/*.mp3

# Design tool workspace (local mockups & asset drafts)
.design/
