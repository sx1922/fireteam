# 047 架构稳定性修复

日期：2026-08-28

## 修复

- 设定包激活改为先加载并校验核心数据，失败时保留旧包，避免热切换进入半初始化状态。
- 客户端模块按 `MODULE_LOAD_PRIORITY` 与字母序加载，和服务端保持一致。
- 设定包支持 `required_data` 声明；未声明时默认要求 `factions/classes/map_rules`。
- `SetScenario` API 拒绝当前设定包不存在的剧本 ID。

## 验证

- 冒烟测试与 Lua 5.1 语法检查通过。
- GMod 实际热切换和客户端模块加载仍需服务器运行时验收。
